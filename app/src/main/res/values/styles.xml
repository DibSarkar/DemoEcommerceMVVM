<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlActivated">@color/change_text_color</item>
        <item name="colorControlNormal">@color/radio_normal</item>

    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/mrp_color</item>
        <item name="colorControlActivated">@color/golden</item>
    </style>

    <style name="RadioButton.Custom" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="colorControlActivated">@color/change_text_color</item>
        <item name="colorControlNormal">@color/radio_normal</item>
    </style>

    <style name="InputLayout" parent="TextAppearance.AppCompat">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/dialog_text</item>
        <item name="android:textColorHint">@color/dialog_text</item>
        <item name="colorAccent">@color/dialog_text</item>
        <item name="colorControlNormal">@color/dialog_text</item>
        <item name="colorControlHighlight">@color/colorPrimaryDark</item>
        <item name="colorControlActivated">@color/colorPrimaryDark</item>
    </style>

    <style name="MyAlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">#fff</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">#fff</item>
        <!-- Used for the background -->
        <item name="android:background">@color/green_color</item>
    </style>
    <style name="SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/bg_spinner</item>
        <item name="android:listPopupWindowStyle">@layout/support_simple_spinner_dropdown_item</item>
    </style>

    <style name="NewDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">true</item>
      <!-- <item name="android:background">@android:color/transparent</item>-->
        <item name="android:progressDrawable">@color/colorPrimary</item>
    </style>

    <style name="bnve">

        <item name="android:textSize">@dimen/design_bottom_navigation_active_text_size</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/_5sdp</item>
    </style>
</resources>
